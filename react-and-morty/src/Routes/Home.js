import React from 'react';

const Home = () => {
    return (
        <div>
            <button className="glow-on-hover" type="button">HOVER ME, THEN CLICK ME!</button>
        </div>
    );
};

export default Home;